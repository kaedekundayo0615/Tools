<UserControl x:Class="PgDbOperator.Views.DbConnectionsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="DB接続管理" FontSize="24" FontWeight="Bold" Margin="0,0,20,0" />
            <Button Content="追加" Command="{Binding AddCommand}" />
            <Button Content="接続テスト" Command="{Binding TestCommand}" />
            <Button Content="保存" Command="{Binding SaveCommand}" />
            <Button Content="削除" Command="{Binding DeleteCommand}" />
        </StackPanel>
        <Grid Grid.Row="1" Margin="0,12,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="3*"/></Grid.ColumnDefinitions>
            <DataGrid ItemsSource="{Binding Connections}" SelectedItem="{Binding SelectedConnection}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="接続名" Binding="{Binding Name}" Width="2*" />
                    <DataGridTextColumn Header="環境" Binding="{Binding EnvironmentType}" Width="*" />
                    <DataGridTextColumn Header="DB名" Binding="{Binding DatabaseName}" Width="*" />
                    <DataGridTextColumn Header="ホスト" Binding="{Binding Host}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
            <ScrollViewer Grid.Column="1">
                <GroupBox Header="詳細">
                    <Grid DataContext="{Binding SelectedConnection}">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="150"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="対象アプリ"/><ComboBox Grid.Column="1" ItemsSource="{Binding DataContext.Applications, RelativeSource={RelativeSource AncestorType=UserControl}}" SelectedValue="{Binding ApplicationId}" SelectedValuePath="Id" DisplayMemberPath="Name" />
                        <TextBlock Grid.Row="1" Text="接続名"/><TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Name}" />
                        <TextBlock Grid.Row="2" Text="環境区分"/><ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding DataContext.EnvironmentTypes, RelativeSource={RelativeSource AncestorType=UserControl}}" SelectedItem="{Binding EnvironmentType}" />
                        <TextBlock Grid.Row="3" Text="ホスト"/><TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Host}" />
                        <TextBlock Grid.Row="4" Text="ポート"/><TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Port}" />
                        <TextBlock Grid.Row="5" Text="DB名"/><TextBox Grid.Row="5" Grid.Column="1" Text="{Binding DatabaseName}" />
                        <TextBlock Grid.Row="6" Text="ユーザー"/><TextBox Grid.Row="6" Grid.Column="1" Text="{Binding UserName}" />
                        <TextBlock Grid.Row="7" Text="パスワード"/><TextBox Grid.Row="7" Grid.Column="1" Text="{Binding DataContext.Password, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Grid.Row="8" Text="保存方式"/><ComboBox Grid.Row="8" Grid.Column="1" ItemsSource="{Binding DataContext.PasswordSaveTypes, RelativeSource={RelativeSource AncestorType=UserControl}}" SelectedItem="{Binding PasswordSaveType}" />
                        <TextBlock Grid.Row="9" Text="安全レベル"/><ComboBox Grid.Row="9" Grid.Column="1" ItemsSource="{Binding DataContext.SafetyLevels, RelativeSource={RelativeSource AncestorType=UserControl}}" SelectedItem="{Binding SafetyLevel}" />
                        <TextBlock Grid.Row="10" Text="SSL"/><CheckBox Grid.Row="10" Grid.Column="1" IsChecked="{Binding UseSsl}" />
                        <TextBlock Grid.Row="11" Text="説明"/><TextBox Grid.Row="11" Grid.Column="1" Text="{Binding Description}" />
                    </Grid>
                </GroupBox>
            </ScrollViewer>
        </Grid>
        <TextBlock Grid.Row="2" Text="{Binding Message}" Foreground="DarkGreen" />
    </Grid>
</UserControl>
